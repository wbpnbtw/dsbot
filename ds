import random
import discord
from discord.ext import commands

config = {
    'token': 'token',
    'prefix': '&',
}
intents = discord.Intents.default()
intents.message_content = True
bot = commands.Bot(command_prefix='!', intents=intents)


@bot.command()
async def w1(ctx, *arg):
    await ctx.reply("smt")


@bot.command()
async def wbpn(ctx, *arg):
    await ctx.reply("smt")


bot.run("token")
